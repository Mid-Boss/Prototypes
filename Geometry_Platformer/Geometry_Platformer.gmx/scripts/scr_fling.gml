/// scr_fling(object id)

the_object = argument0;

with the_object
{
    flung = true;
    /*v_x = 10;
    v_y = -5;
    t = 0;
    sigma = 30;
    grav = 5;
    x_i = x;
    y_i = y;
    while flung
    {
        x = v_x * t * cos(degtorad(sigma)) + x_i;
        y = (v_y * t * sin(degtorad(sigma))) + (0.5 * grav * sqr(t)) + y_i;
        t += 1/room_speed;
        ocollide = place_meeting(x, y, obj_collision_parent);
        //if ocollide != noone
          //  flung = false;
    }*/
}


